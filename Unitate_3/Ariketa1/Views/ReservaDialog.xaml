<Window x:Class="Ariketa1.Views.ReservaDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reservar asiento" Height="220" Width="360" WindowStartupLocation="CenterOwner">
	<Grid Margin="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<TextBlock Text="Izena:"/>
		<TextBox x:Name="TxtNombre" Grid.Row="1" Margin="0,4,0,8" />

		<StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Center">
			<TextBlock Text="Data eta Ordua:" VerticalAlignment="Center" Margin="0,0,8,0"/>
			<DatePicker x:Name="DpFecha" SelectedDate="{x:Static sys:DateTime.Now}" xmlns:sys="clr-namespace:System;assembly=mscorlib"/>
			<TextBox x:Name="TxtHora" Width="60" Margin="8,0,0,0" Text="12:00"/>
		</StackPanel>

		<StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
			<Button Content="Utzi" Click="BtnCancelar_Click" Width="80" Margin="0,0,8,0"/>
			<Button Content="Erreserba" Click="BtnReservar_Click" Width="80"/>
		</StackPanel>
	</Grid>
</Window>
