<UserControl x:Class="Ariketa1.Views.SeatControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="80" Height="80">
    <UserControl.Resources>
        <Style x:Key="SeatButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="LightGreen"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="Padding" Value="4"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="Bd"
                                    CornerRadius="6"
                                    BorderThickness="1"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.05" ScaleY="1.05"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <DataTrigger Binding="{Binding IsOccupied}" Value="True">
                    <Setter Property="Background" Value="#d9534f"/>
                    <Setter Property="BorderBrush" Value="#b52a2a"/>
                    <Setter Property="ToolTip" Value="{Binding ReservedBy, StringFormat=Reservado por: {0}}" />
                </DataTrigger>

                <DataTrigger Binding="{Binding IsOccupied}" Value="False">
                    <Setter Property="Background" Value="LightGreen"/>
                    <Setter Property="BorderBrush" Value="Gray"/>
                    <Setter Property="ToolTip" Value="Disponible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <Button Style="{StaticResource SeatButtonStyle}"
            Click="Button_Click"
            MouseEnter="Button_MouseEnter"
            MouseLeave="Button_MouseLeave"
            Margin="4">
        <StackPanel>
            <TextBlock Text="{Binding Id}" FontWeight="Bold" FontSize="12" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding StatusText}" FontSize="10" HorizontalAlignment="Center"/>
        </StackPanel>
    </Button>
</UserControl>
